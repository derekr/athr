{
  "name": "athr",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build --compile src/cli.ts --outfile athr",
    "test": "bun test",
    "lint": "oxlint src/",
    "lint:fix": "oxlint --fix src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@starfederation/datastar-sdk": "^1.0.0-RC.3",
    "evlog": "^2.12.0",
    "hono": "^4.12.12",
    "music-metadata": "^11.12.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "oxlint": "^1.59.0"
  }
}
